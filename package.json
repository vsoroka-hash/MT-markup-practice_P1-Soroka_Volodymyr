{
  "name": "flora-project",
  "version": "1.0.0",
  "description": "Flora project scope 2",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
